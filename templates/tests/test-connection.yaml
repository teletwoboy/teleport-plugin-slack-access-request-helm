apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "slack-access-request.fullname" . }}-test-connection"
  labels:
    {{- include "slack-access-request.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "slack-access-request.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
