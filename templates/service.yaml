apiVersion: v1
kind: Service
metadata:
  name: {{ include "slack-access-request.fullname" . }}
  labels:
    {{- include "slack-access-request.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slack-access-request.selectorLabels" . | nindent 4 }}
